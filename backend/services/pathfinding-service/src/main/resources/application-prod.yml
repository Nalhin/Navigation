server:
  port: ${SERVER_PORT}

spring:
  application:
    name: pathfinding-service
  data:
    mongodb:
      database: ${MONGO_DATABASE}
      port: ${MONGO_PORT}
      host: ${MONGO_HOST}

  cloud:
    consul:
      host: ${CONSUL_HOST}
      port: ${CONSUL_PORT}

